# Contributing

Use Python 3.11+ and Node 22+. Install with `make install`, run `make test`, `make lint`, and `make typecheck` before opening a pull request. Benchmark artifacts must state whether they are verified Arm64 measurements or development fixtures. Never commit model weights or claim unmeasured performance.

